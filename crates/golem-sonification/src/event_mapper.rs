//! Maps `GolemEvent` payloads to sonification triggers.
//!
//! Stub: will translate event fabric events into one-shot module triggers,
//! gate signals, and parameter changes in future plans.

/// Placeholder for event-to-sonification mapping.
pub struct EventMapper;

impl EventMapper {
    /// Create a new event mapper.
    pub fn new() -> Self {
        Self
    }
}

impl Default for EventMapper {
    fn default() -> Self {
        Self::new()
    }
}
